

export default (
  function() {
    this.data = (
      Buffer.alloc(
        this.codec_id =
        this.codec_type =
        this.duration =
        this.flags =
        this.pts =
        this.dts =
        this.size =
        this.offset = 0
      )
    );
  }
)