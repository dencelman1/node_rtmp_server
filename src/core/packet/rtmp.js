

export default (
    function (
        f,
        c
    ) {
        this.fmt = f;
        this.cid = c;
        
        this.payload = Buffer.alloc(
            this.timestamp =
            this.length =
            this.type =
            this.stream_id =
            this.clock =
            this.capacity =
            this.bytes = 0
        );
    }
);
