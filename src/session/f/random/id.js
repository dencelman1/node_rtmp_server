

export default (
    
    function(size) {
        var
            id = "",
            i = 0
        ;
        while (true) {
            i = step;
            while (i--) {
                if (
                    (
                        id += this.alphabet[p.random(this.step)[i] & this.mask] || ""
                    )
                    .length >= (
                        size
                        ||
                        this.defaultSize
                    )
                ) return id;
            }
        }
    }
    
)